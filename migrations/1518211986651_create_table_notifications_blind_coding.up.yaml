- args:
    sql: CREATE TABLE "notifications_blind_coding"("id" serial NOT NULL, "description"
      timestamptz NOT NULL DEFAULT now(), PRIMARY KEY ("id") )
  type: run_sql
- args:
    name: notifications_blind_coding
  type: add_existing_table_or_view
