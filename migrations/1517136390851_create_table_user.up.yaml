- args:
    sql: CREATE TABLE "user"("user_id" integer NOT NULL, "email" text NOT NULL, "name"
      text NOT NULL, "College" text , "mobile" text , "branch" text , "semester" text
      , PRIMARY KEY ("user_id") )
  type: run_sql
- args:
    name: user
  type: add_existing_table_or_view
